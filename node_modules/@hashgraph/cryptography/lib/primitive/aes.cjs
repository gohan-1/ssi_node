"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createCipheriv = createCipheriv;
exports.createDecipheriv = createDecipheriv;
exports.CipherAlgorithm = void 0;
const CipherAlgorithm = {
  Aes128Ctr: "AES-128-CTR",
  Aes128Cbc: "AES-128-CBC"
};
/**
 * @param {string} algorithm
 * @param {Uint8Array} key
 * @param {Uint8Array} iv
 * @param {Uint8Array} data
 * @returns {Promise<Uint8Array>}
 */

exports.CipherAlgorithm = CipherAlgorithm;

async function createCipheriv(algorithm, key, iv, data) {
  const cipher = (await Promise.resolve().then(() => require("crypto"))).createCipheriv(algorithm, key.slice(0, 16), iv);
  return Buffer.concat([cipher.update(data), cipher["final"]()]);
}
/**
 * @param {string} algorithm
 * @param {Uint8Array} key
 * @param {Uint8Array} iv
 * @param {Uint8Array} data
 * @returns {Promise<Uint8Array>}
 */


async function createDecipheriv(algorithm, key, iv, data) {
  const decipher = (await Promise.resolve().then(() => require("crypto"))).createDecipheriv(algorithm, key.slice(0, 16), iv);
  return Buffer.concat([decipher.update(data), decipher["final"]()]);
}